import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#40A578", // Replace with your main color
    },
    secondary: {
      main: "#9DDE8B", // Replace with your secondary color
    },
  },
});
export default theme;
